# 变更记录

## v1.0.1 — 2026-09-05（缺陷修复）

GUI 显示修复：

- 修复左侧列表条目文字上下被压/被裁：条目高度按真实渲染字体行高计算（含字形富余与顶部留白），文字不再出现半截字
- 修复左侧列表超长标题/账号被硬截断：超长文字显示尾部省略号，悬停可查看完整内容
- 修复右侧详情排版不齐：清零子布局默认边距，标题 / 字段行 / 备注 / 按钮纵向对齐同一列线
- 修复右侧详情长内容显示不全：长标题与长账号/链接自动换行完整显示

## v1.0.0 — 2026-09-04（正式版）

首个正式版。纯本地离线密码管理器，GUI（PySide6）+ CLI 双形态。

### 功能
- Argon2id + AES-256-GCM 双层密钥加密，单文件 `.pbk` 原子写 + 2 份轮转备份
- GUI：citrus 酸橙信笺风格，建库/解锁/搜索/增删改查/生成强密码/改主密码/自动锁定
- CLI：全部管理命令 + 导入导出 + `recover` 备份恢复
- 库文件固定放程序旁（便携 U 盘场景），界面不提供选路径
- 登录账号智能标签：邮箱 / 手机号 / 用户名随内容自动识别
- 一键导出明文副本：JSON 完整还原 或 CSV 通用迁移格式

### 安全
- 会话主密码 bytearray 缓存，锁定逐字节清零；5 分钟无操作自动锁定
- 复制密码 45s 自动清空剪贴板（仅未被改写时）
- 改主密码后自动清除仍用旧密码加密的轮转备份
- 文件头 KDF 参数上限校验（防恶意文件内存耗尽 DoS）
- CSV 导出公式注入防护（`= + - @` 前缀字段加引号）
- 剪贴板/错误信息/日志全程不落明文密码

### 已知取舍
- `Entry.data` 为不可变 `str`，条目级敏感字段无法覆写清零（DESIGN.md §12）
- 未收紧文件 ACL：库文件本身加密，且便携跨机场景不适用（DESIGN.md §7）
